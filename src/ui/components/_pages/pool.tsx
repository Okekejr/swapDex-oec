import { TokenItems } from "@/ui/core/tokenItems";
import { HStack, Text } from "@chakra-ui/react";

const Pool = () => {
  return (
    <HStack>
      <Text>Pool</Text>
      <TokenItems />
    </HStack>
  );
};

export default Pool;
